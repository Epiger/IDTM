{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "msbuild",
            "type": "shell",
            "args": [
                "${workspaceFolder}/idtm.csproj",
                "/restore:True",
                "/p:Configuration=Debug",
                "/p:Platform=x64"

            ],
        }
    ]
}